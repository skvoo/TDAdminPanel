# Vercel: Project → Settings → Environment Variables → Import (.env)
# 1. Copy this file to .env (or rename), replace placeholders with real values.
# 2. Do NOT commit .env. Add Production (and Preview) when importing.
# 3. After import, trigger a redeploy so serverless functions get the new vars.

DATABASE_URL=postgresql://tdadmin:YOUR_PASSWORD@104.223.25.234:6432/td
S3_ENDPOINT=http://104.223.25.234:9000
S3_BUCKET=td-tickets
S3_ACCESS_KEY=s3user_td
S3_SECRET_KEY=YOUR_MINIO_SECRET
S3_PUBLIC_URL=http://104.223.25.234:9000/td-tickets
JWT_SECRET=your-random-secret-at-least-32-chars
